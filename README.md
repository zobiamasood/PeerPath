
  # Continue screen generation

  This is a code bundle for Continue screen generation. The original project is available at https://www.figma.com/design/1JS8RJ2TK634izmwOaQ76A/Continue-screen-generation.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  